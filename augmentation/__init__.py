"""Stage 2 — Augmentation (ARCHITECTURE §2.3).

External-API code path (Claude / Gemini / GPT-4 class). This is deliberately
separate from `models/` (which is HF-only) — see README §9.7 and
ARCHITECTURE §7.
"""
